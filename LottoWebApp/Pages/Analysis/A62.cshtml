@page
@model LottoWebApp.Pages.Analysis.A62Model
@{
    ViewData["Title"] = "6.2 Диагональные линии слева направо/справа налево";
}

<div class="container mt-4">
    <h2 class="mb-4">@ViewData["Title"]</h2>

    <a href="/User/UserMainMenu" class="btn btn-secondary mb-4">⬅ Назад в меню</a>

    <form method="post" class="mb-4">
        <input type="hidden" name="Game" value="@Model.Game" />
        <div class="row">
            <div class="col-md-3">
                <label for="DrawCount" class="form-label">Количество тиражей (0 - все)</label>
                <input type="number" name="DrawCount" id="DrawCount" class="form-control" min="0" value="@(Model.DrawCount > 0 ? Model.DrawCount : 0)" />
            </div>
            <div class="col-md-3">
                <label for="Direction" class="form-label">Направление</label>
                <select name="Direction" id="Direction" class="form-select">
                    <option value="newToOld" selected="@(Model.Direction == "newToOld")">Сначала новые</option>
                    <option value="oldToNew" selected="@(Model.Direction == "oldToNew")">Сначала старые</option>
                </select>
            </div>
            <div class="col-md-3">
                <label for="MinLineLength" class="form-label">Мин. длина линии</label>
                <input type="number" name="MinLineLength" id="MinLineLength" class="form-control" min="2" value="@(Model.MinLineLength > 1 ? Model.MinLineLength : 2)" />
            </div>
            <div class="col-md-3">
                <label for="LineType" class="form-label">Тип линии</label>
                <select name="LineType" id="LineType" class="form-select">
                    <option value="leftToRight" selected="@(Model.LineType == "leftToRight")">Слева направо</option>
                    <option value="rightToLeft" selected="@(Model.LineType == "rightToLeft")">Справа налево</option>
                </select>
            </div>
            <div class="col-md-12 mt-2">
                <button type="submit" class="btn btn-primary">Найти диагональные линии</button>
                @if (Model.DiagonalLines != null)
                {
                    <button type="submit" asp-page-handler="DownloadCsv" class="btn btn-success ms-2">💾 Скачать CSV</button>
                }
            </div>
        </div>
    </form>

    @if (Model.DiagonalLines != null)
    {
        <div class="table-responsive">
            <table class="table table-dark table-bordered text-center">
                <thead>
                    <tr>
                        <th>Начальный тираж</th>
                        <th>Конечный тираж</th>
                        <th>Начальная позиция</th>
                        <th>Конечная позиция</th>
                        <th>Числа</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var line in Model.DiagonalLines)
                    {
                        <tr>
                            <td>@line.StartDraw</td>
                            <td>@line.EndDraw</td>
                            <td>@line.StartPosition</td>
                            <td>@line.EndPosition</td>
                            <td>@line.Numbers</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>